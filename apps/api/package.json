{
  "name": "@saga-agent-ops/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for api yet.\"",
    "test": "echo \"No tests yet for api.\""
  },
  "dependencies": {
    "@hono/node-server": "latest",
    "@saga-agent-ops/agent-runtime": "workspace:*",
    "@saga-agent-ops/db": "workspace:*",
    "@saga-agent-ops/shared": "workspace:*",
    "drizzle-orm": "latest",
    "hono": "latest",
    "postgres": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "latest"
  }
}
